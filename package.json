{
  "name": "coordinates",
  "version": "1.0.0",
  "description": "A simple library to convert polar, cartesian, and canvas coordinates.",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx rimraf ./dist && npx tsc",
    "test": "npx jest",
    "coverage": "npx jest --coverage",
    "pack": "npx rimraf *.tgz && npm pack"
  },
  "keywords": [],
  "author": "Dennis Dunn <ansofive@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.13",
    "jest": "^26.1.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
